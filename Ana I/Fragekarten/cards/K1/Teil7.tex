\card{Wie ist der Betrag einer Zahl $x\in \R$ definiert}{102}{
	$|x|:=\left\{\begin{array}{lr}
		x,&x\geq 0\\
		-x,&x<0
	\end{array}\right.$
}
\card{Was ist die Länge eines Intervalls $I\subset\R$}{102}{
	Sei $I$ ein Intervall mit Endpunkten $a,b$. Dann ist $|a-b|$ die Länge von $I$.
}
\card{Was ist die Wurzel einer Zahl $a$}{107}{
	$\sqrt{a}$ ist die Zahl $a\in\R_+$ mit $(\sqrt{a})^2=a$.
}